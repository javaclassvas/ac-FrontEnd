import React from "react";
import styles from "components/chat/chat-info/chat-info.module.scss";

const ChatInfo = (): React.ReactElement => (
  <div className={styles.cont}>
    <h5>ANYCLIP PUBLIC CHAT</h5>
  </div>
);

export default ChatInfo;
